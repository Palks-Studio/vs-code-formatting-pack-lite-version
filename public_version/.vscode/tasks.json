{
    // ==============================
    // VS CODE TASKS (LITE VERSION)
    // ==============================
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Margin detection (read-only)",
            "type": "shell",
            "detail": "Analyzes files and reports margin issues without modifying them",
            "command": "${config:python.defaultInterpreterPath}",
            "args": [
                "space.py"
            ],
            "presentation": {
                "reveal": "always",
                "panel": "dedicated",
                "clear": true
            },
            "problemMatcher": []
        }
    ]
}
